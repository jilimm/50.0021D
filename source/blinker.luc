module blinker (
    input clk,    // clock
    input rst,    // reset
    output blink[3]
  ) {
  
  dff counter[3](.clk(clk), .rst(rst));

  always {
    blink = counter.q;
    counter.d = counter.q + 1;
  }
}
